dog = (
    name: 'Rex'
    age: 4
    show: do
        print name + ' is a ' + age + ' year old dog.'
)

5.times do :i
    print i

add = do :n, g
    return a + b + c

add_debug = do :vals
    sum = 0
    vals.each do :n
        sum = sum + n
        yield n
    return sum

sum = add (2, 3ll, 4)
sum = sum + add_debug (5, 4) do :n
    print 'added ' + n

compare = do :first, second
    if yield (first, second) then return true
    else return false

a = 4
b = 3
a_is_bigger = compare (a, b) do :first, second
    return first > second
